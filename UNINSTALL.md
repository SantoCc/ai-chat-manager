# 扩展已移除 — 请确认是否已备份

你刚才在浏览器扩展页点了「移除」，**AI对话管理器**已被卸下。

## 重要

本扩展的对话数据保存在浏览器本地。**移除后这些数据会一并清空，无法自动恢复。**

## 若还有机会（其实已经移除了）

下次使用前请养成习惯：

1. 打开扩展侧栏 → **设置** → **数据管理**
2. 点击 **导出 JSON 备份**，把文件存到电脑
3. 确认备份能打开后，再在 `edge://extensions/` 中点「移除」

## 需要找回数据时

- 若移除前导出过 JSON：重新安装扩展后，在设置里用 **合并导入** 或 **替换导入**
- 若从未导出：本地数据通常无法找回

## 重新安装

仓库：https://github.com/SantoCc/ai-chat-manager  
Edge：`edge://extensions/` → 开发人员模式 → 加载解压缩的扩展
